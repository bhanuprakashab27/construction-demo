# Server
server.port=8080

# App Name
spring.application.name=sthapatya

# MongoDB (IMPORTANT - use env variable)
spring.data.mongodb.uri=${MONGO_URI}
spring.data.mongodb.database=sthapatya

# File Upload (optional but recommended)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

#spring.data.mongodb.uri=mongodb://localhost:27017/sthapatya_db
#spring.application.name=sthapatya
#server.port=8080


